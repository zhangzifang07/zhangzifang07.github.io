# 个人博客项目交接文档

## 一、项目基本信息

| 项目 | 说明 |
|------|------|
| 项目名称 | zhangzifang 的个人博客 |
| 框架 | Hexo 8.1.2 |
| 主题 | Fluid |
| 部署方式 | hexo-deployer-git → GitHub Pages（main 分支） |
| 源码托管 | Git → 同仓库 hexo 分支 |
| 访问地址 | https://zhangzifang07.github.io |
| GitHub 仓库 | git@github.com:zhangzifang07/zhangzifang07.github.io.git |
| 本地路径 | F:\zhangzifang07 |
| 作者 | zhangzifang |

---

## 二、项目结构

```
F:\zhangzifang07\
├── .github/
│   └── dependabot.yml          # Dependabot 自动依赖更新
├── scaffolds/                  # 文章模板
│   ├── draft.md                #   草稿模板
│   ├── page.md                 #   页面模板
│   └── post.md                 #   文章模板
├── source/                     # 博客内容源文件
│   ├── _posts/                 #   文章目录
│   │   ├── Linux学习.md
│   │   ├── GitHub的上传与下载.md
│   │   ├── E10开发环境搭建以及打包.md
│   │   └── E10开发环境搭建以及打包/  # 文章同名图片文件夹
│   ├── about/                  #   关于页面
│   │   └── index.md
│   └── img/                    #   全局图片资源
│       ├── wechat-qrcode.png   #   微信二维码
│       └── qq-qrcode.png       #   QQ 邮箱展示图
├── themes/                     # 主题目录（空，使用 npm 安装的 Fluid）
├── _config.yml                 # Hexo 主配置文件
├── _config.fluid.yml           # Fluid 主题配置文件
├── package.json                # 项目依赖与脚本
└── README.md                   # 项目说明文档
```

---

## 三、已安装的依赖

| 依赖 | 用途 |
|------|------|
| hexo ^8.0.0 | 核心框架 |
| hexo-generator-archive | 归档页生成 |
| hexo-generator-category | 分类页生成 |
| hexo-generator-index | 首页生成 |
| hexo-generator-tag | 标签页生成 |
| hexo-renderer-ejs | EJS 模板渲染 |
| hexo-renderer-marked | Markdown 渲染 |
| hexo-renderer-stylus | Stylus CSS 渲染 |
| hexo-server | 本地开发服务器 |
| hexo-theme-landscape | 默认主题（已弃用） |
| hexo-theme-fluid | 当前使用主题 |
| hexo-deployer-git | Git 部署插件 |

---

## 四、已完成的修改记录

### 4.1 站点配置（_config.yml）

| 配置项 | 默认值 | 修改后 |
|--------|--------|--------|
| title | Hexo | Zhang的个人博客 |
| author | John Doe | zhangzifang |
| language | en | zh-CN |
| timezone | '' | Asia/Shanghai |
| url | http://example.com | https://zhangzifang07.github.io |
| post_asset_folder | false | true |
| theme | landscape | fluid |
| deploy.type | '' | git |
| deploy.repository | — | git@github.com:zhangzifang07/zhangzifang07.github.io.git |
| deploy.branch | — | main |

### 4.2 主题配置（_config.fluid.yml）

| 配置项 | 默认值 | 修改后 |
|--------|--------|--------|
| navbar.blog_title | "Fluid" | "zhangzifang的个人博客" |
| navbar.menu archive | key: "archive" | 增加 name: "文章" |
| footer.content | Hexo ❤ Fluid | © 2026 zhangzifang ❤ Fluid |
| index.slogan.text | "An elegant Material-Design theme for Hexo" | "记录技术成长，分享生活感悟" |
| about.name | "Fluid" | "zhangzifang" |
| about.intro | "An elegant theme for Hexo" | "热爱技术，热爱生活" |
| about.icons | GitHub + 豆瓣 + 微信 | GitHub + QQ邮箱(悬浮) + 微信(悬浮) |
| custom_css | 空 | 引入 FontAwesome 6.5.1 图标库 |

### 4.3 内容相关

| 操作 | 说明 |
|------|------|
| 添加关于页面 | source/about/index.md，包含个人介绍和 GitHub 链接 |
| 添加文章 | Linux学习.md、GitHub的上传与下载.md、E10开发环境搭建以及打包.md |
| 图片管理 | 开启 post_asset_folder，E10 文章使用同名文件夹管理图片 |
| 图片引用语法 | 使用 `{% asset_img "图片名" 描述 %}` 代替标准 Markdown 语法 |
| 文章链接 | 使用 permalink 字段自定义英文链接，避免中文路径 |

### 4.4 版本管理

| 操作 | 说明 |
|------|------|
| Git 初始化 | 在项目根目录 `git init`，创建 hexo 分支 |
| 源码远程托管 | 推送到 git@github.com:zhangzifang07/zhangzifang07.github.io.git 的 hexo 分支 |
| .gitignore 配置 | 排除 node_modules/、public/、.deploy_git/、db.json 等生成文件 |

### 4.5 全局图片资源

| 文件 | 用途 |
|------|------|
| source/img/wechat-qrcode.png | 关于页微信悬浮二维码 |
| source/img/qq-qrcode.png | 关于页 QQ 邮箱悬浮展示图 |

---

## 五、常用操作指南

### 5.1 本地预览

```bash
hexo s
# 访问 http://localhost:4000
```

### 5.2 写新文章

```bash
hexo new "文章标题"
# 编辑 source/_posts/文章标题.md
# 图片放入 source/_posts/文章标题/ 文件夹
# 图片引用：{% asset_img "图片名.png" 描述 %}
```

### 5.3 文章头部模板

```markdown
---
title: 文章中文标题
date: 2026-06-29 16:00:00
permalink: /2026/06/29/english-url-name/
tags:
  - 标签1
  - 标签2
categories:
  - 分类名
---
```

### 5.4 部署上线

```bash
hexo g -d
# 等待 1-2 分钟 GitHub Pages 生效
```

### 5.5 清理缓存

```bash
hexo clean
# 切换主题或遇到渲染异常时使用
```

---

## 六、仓库与版本管理

### 6.1 仓库双分支结构

本项目使用**同一个 GitHub 仓库的两个分支**分别管理部署文件和源码：

```
zhangzifang07.github.io（GitHub 仓库）
│
├── main 分支    ← 博客静态网页（由 hexo g -d 自动推送 public/ 内容）
│                  包含：HTML、CSS、JS 等生成文件
│                  ⚠️ 不要手动编辑此分支的内容，它由 Hexo 全权管理
│
└── hexo 分支    ← 博客源码（由 git push 手动推送）
                   包含：文章 .md、配置 _config.yml、主题配置、图片等
                   ✅ 日常开发在此分支操作
```

### 6.2 两套管理工具的职责划分

| 操作 | 管理工具 | 作用分支 | 说明 |
|------|----------|----------|------|
| 写文章、改配置 | **Git** | hexo | 手动 `git add → commit → push` |
| 预览博客 | **Hexo** | 本地 | `hexo s`，与远程无关 |
| 部署上线 | **Hexo** | main | `hexo g -d`，自动推送到 main |
| 回滚源码 | **Git** | hexo | `git revert` / `git reset` |
| 回滚网页 | **Hexo** | main | 修复源码后重新 `hexo g -d` 覆盖 |

> **关键理解**：`hexo g -d` 只管 `public/` → `main` 分支；`git push` 只管源码 → `hexo` 分支。两者互不影响。

### 6.3 日常操作流程

```
写文章/改配置 → hexo s 本地预览 → hexo g -d 部署上线 → git add/commit/push 备份源码
```

具体命令：

```bash
# 1. 写文章（Hexo 管理）
hexo new "文章标题"

# 2. 本地预览（Hexo 管理）
hexo s

# 3. 部署上线（Hexo → main 分支）
hexo g -d

# 4. 备份源码（Git → hexo 分支）
git add -A
git commit -m "新增文章：xxx"
git push
```

> 💡 **建议**：每次 `hexo g -d` 部署后，顺手 `git push` 备份源码，养成习惯确保源码不丢失。

### 6.4 换电脑恢复博客完整步骤

当需要在新的电脑上继续开发博客时，按以下步骤操作：

**前置条件**：新电脑已安装 Node.js、Git，并配置好 SSH 密钥（能访问 GitHub）

```bash
# 1. 克隆 hexo 分支（源码）到本地
git clone -b hexo git@github.com:zhangzifang07/zhangzifang07.github.io.git

# 2. 进入项目目录
cd zhangzifang07.github.io

# 3. 安装依赖（node_modules 被 .gitignore 排除了，需要重新安装）
npm install

# 4. 安装 Fluid 主题（通过 npm 安装的，需要重新安装）
npm install hexo-theme-fluid

# 5. 本地预览确认一切正常
hexo s

# 6. 之后正常写文章、部署、推送源码即可
```

> ⚠️ **重点提醒**：
> - `npm install` 会根据 `package.json` 和 `package-lock.json` 恢复所有依赖，包括 Fluid 主题
> - **不要**克隆 main 分支，那个只有生成的静态文件，没有源码
> - 如果 SSH 密钥未配置，需要先运行 `ssh-keygen` 生成密钥，并将公钥添加到 GitHub

---

## 七、注意事项

1. **文件编码**：所有 `.yml` 和 `.md` 文件必须使用 **UTF-8** 编码，中文才不会乱码
2. **修改主题配置**：修改 `_config.fluid.yml` 后必须**重启** `hexo s` 才能生效
3. **修改文章内容**：修改 `source/_posts/` 下的 `.md` 文件，保存后刷新浏览器即可生效
4. **图片引用**：文章图片必须使用 `{% asset_img "图片名" 描述 %}` 语法，Obsidian 的 `![[]]` 和标准 Markdown 的 `![]()` 对文章资源文件夹支持不好
5. **中文链接**：文章链接中包含中文会导致访问失败，务必在头部用 `permalink` 指定英文路径
6. **部署 vs 源码**：`hexo g -d` 推送 `public/` 到 main 分支，源码需手动 `git push` 到 hexo 分支，两者互不影响
7. **FontAwesome 图标**：已通过 custom_css 引入 FontAwesome 6.5.1，关于页的邮箱图标使用了 `fa-solid fa-envelope` 类名

---

## 八、参考文档

- [Hexo 官方文档](https://hexo.io/docs/)
- [Fluid 主题配置指南](https://hexo.fluid-dev.com/docs/guide/)
- [Fluid 主题图标参考](https://hexo.fluid-dev.com/docs/icon/)
- [FontAwesome 图标库](https://fontawesome.com/icons)
